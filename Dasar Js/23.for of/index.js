// di gunakan untuk mengambil isi dari properti dari sebuah array atau string

const nama = ["kz", "sudrun", "bejo"];

for (let a of nama) {
  console.log(a);
}

const fulname = "sudrun kz bejo";

for (let b of fulname) {
  console.log(b);
}
